import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samurai.app',
  appName: 'Samurai Strikers',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
